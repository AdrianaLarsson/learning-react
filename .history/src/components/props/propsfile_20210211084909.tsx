interface Header {
    testObject: {}
}

const Header = (props: any, props) => {

    


 return (
     <header>
     Header {props.title}
     {props.name}

    
     </header>
 )
}

export default Header;