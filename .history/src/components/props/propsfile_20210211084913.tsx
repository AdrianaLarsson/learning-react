interface Header {
    testObject: {}
}

const Header = (props: any, pro) => {

    


 return (
     <header>
     Header {props.title}
     {props.name}

    
     </header>
 )
}

export default Header;