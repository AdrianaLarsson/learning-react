interface Header {
    testObject: {}
}

const Header = (props: any, prop: Header) => {

 pro


 return (
     <header>
     Header {props.title}
     {props.name}

    
     </header>
 )
}

export default Header;