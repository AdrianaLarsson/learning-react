interface Header {
    testObject: {}
}

const Header = (props: any, prop: He) => {

    


 return (
     <header>
     Header {props.title}
     {props.name}

    
     </header>
 )
}

export default Header;