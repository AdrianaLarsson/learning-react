import React from 'react'

function Arrays() {

    let selectedColors = ['red','blue'];
    console.log('selectedColors: ', selectedColors);
    const r selectedColors[0]

    return (
        <div>Arrays</div>
    )
}

export default Arrays;