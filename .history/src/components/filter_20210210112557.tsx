import React from "react";

function Filter() {
    const objadriana = [
        {id:4, name:'adriana'},
        {id:8, name:'pedroza'},
        {id:9, name:'larsson'},
    ]
     const a = name
    const filt = objadriana.filter((n)=> n.name === name)

  return <div>Filter</div>;
}

export default Filter;
